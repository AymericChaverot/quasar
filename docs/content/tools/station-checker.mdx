---
title: Station checker
description: "Read a station document before installing it: what Quasar would refuse, and what you would be accepting."
full: true
---

import StationChecker from '../../components/StationChecker.astro';

Paste a station document. This runs the checks Quasar runs before storing one, and shows the permissions in the same plain words the install screen uses — which is the part worth reading when somebody sends you a station.

Nothing is installed and nothing leaves this page. Writing one instead? Use the [station builder](/tools/station-builder/).

<StationChecker />
